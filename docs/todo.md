# TODO — что доделать по мелочи

Список накопившихся улучшений. Прод работает, эти пункты — для повышения автоматизации и удобства.

## Zerocoder — точные дефолты UTM

Сейчас часть значений из ТЗ требует ручной правки в финальном «паровозике». Можно автоматизировать, если добавить поле `default_campaign` в таблицу `channels` (по аналогии с `default_content` и `default_term`).

- [ ] **Геткурс «Все тренинги»** — `utm_campaign = training` (фикс). Сейчас подставляется тег сайта или хвост URL → пользователь правит руками
- [ ] **Геткурс «Запись вебинара / Интенсив / Урок / Баннер»** — campaign = «тег вебинара / тег курса / тег активности» (это и есть тег сайта, поэтому работает само)
- [ ] **Внешние выступления** — `utm_content = имя спикера`. Сейчас стоит литерал «спикер», пользователь меняет
- [ ] **Блог** — `utm_content = url статьи` (заменяется руками)
- [ ] **Воронка — Продажное письмо** — `utm_term = message-N`, где N — номер письма. Сейчас литерал, нужен инпут «номер сообщения» на шаге

## UX-улучшения

- [ ] **Воронка вебинара — отдельный шаг «Шаг/письмо»**. Сейчас все 10 каналов показаны списком с группами (Продажное / Вход / Шальной). Можно сделать сначала выбор «шага», потом канал — будет короче на этапе.
- [ ] **Реклама Zerocoder с плейсхолдерами Яндекса** — `{campaign_id}`, `{ad_id}`, `{keyword}` сохраняются в URL литералом (без `%7B`). Работает. Но было бы удобно показать в превью «эти значения подставит Яндекс автоматически» — подсказка-tooltip.
- [ ] **Реклама — пресет «Я.Директ Вика kab2/kab3»** — в ТЗ три варианта `Yandex_vika_kab2`, `Yandex_vika_kab3`, `Yandex_vika` (разные кабинеты). Сейчас один пресет `yandex / vika`. Можно расширить до 3 кнопок если нужно.

## Инфраструктура

- [ ] **Vercel auto-deploy при push**. Сейчас деплой ручной через `vercel deploy --prod`. Можно привязать GitHub-репо к Vercel-проекту через web-UI ([настройки](https://vercel.com/semanjas-projects/utm-matrius/settings/git)) — тогда `git push origin main` будет триггерить сборку автоматически.
- [ ] **Сменить ADMIN_PASSWORD на проде** с дефолтного `matrius2026`. Команды:
  ```
  vercel env rm ADMIN_PASSWORD production
  vercel env add ADMIN_PASSWORD production
  vercel deploy --prod
  ```

## Админка — недоделки

- [ ] **CRUD людей-рекламщиков** — сейчас «человек» хранится в `group_name` каналов. Можно вынести в отдельную таблицу `people` для:
  - Удобного редактирования имени по всем каналам сразу
  - Отдельной страницы `/admin/people`
- [ ] **Конкретные настройки `task_types` per company** — сейчас захардкожено в `Wizard.tsx`. Можно вынести в БД, чтобы из админки добавлять новые типы без кода.

## Возможные мелочи

- [ ] **Pseudo-локализация** — приложение полностью на русском, в будущем может пригодиться i18n (`next-intl`)
- [ ] **Поиск в админке по `audit_log`** — сейчас фильтр только по таблице. Хорошо было бы по дате, action, JSON-содержимому
- [ ] **Bulk import для каналов** (аналогично сайтам) — массово засеять каналы из CSV/TSV. Сейчас канал добавляется только по одному через форму

## Документация / помощь пользователю

- [ ] **Инструкция на главной по работе с админкой** — короткий блок «что админка умеет» с примерами (CRUD сайтов и каналов, импорт CSV, история с откатом). Можно как сворачиваемая «помощь» в шапке или отдельная страница `/help`

## Из стиля кода

- [ ] **Дублирование между BranchAnonse / BranchSmm / BranchGuide / BranchAds** — много copy-paste. Можно вынести общие шаги (StepSite, StepDate, EditableUtm) в shared компоненты. Не критично, но техдолг растёт
- [ ] **«Middleware deprecated» warning** — Next.js 16 хочет переименовать `src/middleware.ts` → `src/proxy.ts`. Замена тривиальная, можно сделать
